<!DOCTYPE html>
<html>
  <head>
    <title>BlogsByAnimesh</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>


<%= render 'layouts/navbar' %>
<div class="container">
  <% if notice %>
    <p class="alert m-3 alert-info"><%= notice %></p>
  <% end %>

  <% if alert %>
    <p class="alert m-3 alert-warning"><%= alert %></p>
  <% end %>

  <%= yield %>
</div>
  </body>
</html>
